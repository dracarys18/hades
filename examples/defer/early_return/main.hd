import std::io

fn check(x: int): int {
    defer {
        io::printf("deferred\n");
    }

    if (x > 0) {
        io::printf("positive\n");
        return 1;
    }

    io::printf("non-positive\n");
    return 0;
}

fn main(): int {
    check(5);
    check(-1);
    return 0;
}
