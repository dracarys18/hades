import std::math
import self::calculator

fn main(): int {
    let x: int = 10;
    let y: int = 20;
    let result: int = calculator::calculate(x, y);
    let max_val: int = math::max(x, y);
    let final: int = result + max_val;
    printf("%d\n", final);
    return 0;
}
