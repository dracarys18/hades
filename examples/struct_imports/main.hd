import std::io
import self::math_ops

fn main(): int {
    let x = 1;
    let y = 2;
    let arith = math_ops::Arithmetic {a: x, b: y};
    let m = arith.mul();
    io::printf("%d\n", m);
    return 0;
}
