import std::io
import self::intermediate

fn main(): int {
    let x: int = 5;
    let y: int = 10;
    let z: int = 15;
    let result: int = intermediate::add_three(x, y, z);
    io::printf("%d\n", result);
    return 0;
}
