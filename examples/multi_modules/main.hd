import std::io
import self::math_ops
import self::helpers

fn main(): int {
    let x: int = 4;
    let sq: int = math_ops::square(x);
    let dbl: int = helpers::double(x);
    let result: int = sq + dbl;
    io::printf("%d\n", result);
    return 0;
}
