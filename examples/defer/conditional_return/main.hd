import std::io

fn check(x: int): int {
    defer {
        io::printf("deferred\n");
    }

    if (x > 0) {
        return 1;
    } else {
        return 0;
    }
}

fn main(): int {
    check(1);
    check(-1);
    return 0;
}
