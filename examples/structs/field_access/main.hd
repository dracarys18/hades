import std::io
struct Bar {
  foo: string,
  bar: int
}

fn main(): int {
    let b = Bar {foo: "Alice", bar: 3};
    io::printf("Value is %s\n", b.foo);
    io::printf("Value is %d\n", b.bar);
    return 0;
}
