import std::io
fn main():int {
    let count = 0;
    for let i=10;i>0;i-=1 {
        if(count == 3) {
            break;
        }
        count+=1;
    }
    io::printf("%d", count);
    return 0;
}
