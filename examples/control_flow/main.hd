import std::io
fn main():int {
    let a = 2;
    let b = 1;
    let c = a==b;

    if(!c) {
        io::printf("Voila\n");
    } else {
        io::printf("OOf\n");
    }

    if(c) {
        io::printf("OOf\n");
    } else {
        io::printf("Voila\n");
    }
    return 0;
}
