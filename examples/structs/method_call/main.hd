struct Point {
    x: int,
    y: int,

    fn getX(): int {
        return self.x;
    }

    fn getY(): int {
        return self.y;
    }

    fn sum(): int {
        return self.x + self.y;
    }
}

fn main(): int {
    let p = Point { x: 10, y: 20 };
    let x = p.getX();
    let y = p.getY();
    let s = p.sum();
    printf("x=%d y=%d sum=%d\n", x, y, s);
    return 0;
}
