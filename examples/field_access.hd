struct Foo {
  bar: int
}

struct Bar {
  foo: string
}

fn main(): int {
    let b = Bar {foo: "Alice"};
    let c = Bar {foo: "Bob"};
    printf("Value is %s\n", c.foo);
    printf("Value is %s\n", b.foo);
    return 0;
}
