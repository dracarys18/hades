import std::io
import self::geometry

fn main(): int {
    let p = geometry::Point::origin();
    let q = geometry::Point::new(3, 4);
    io::printf("%d\n", p.x);
    io::printf("%d\n", q.x);
    return 0;
}
