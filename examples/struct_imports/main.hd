import std::io
import self::math_ops

fn main(): int {
    let x = 4;
    let y = 6;
    let arith = math_ops::Arithmetic {a: x, b: y};
    let m = arith.mul();
    io::printf("%d\n", m);
    return 0;
}
